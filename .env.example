# Azure Blob Storage Configuration
# Generate a SAS URL with Read permissions from Azure Portal
AZURE_BLOB_SAS_URL=https://yourstorageaccount.blob.core.windows.net/containers?sp=r&st=2024...&se=2024...&spr=https&sv=2021...&sr=c&sig=...

# Docker Hub Configuration
# Your Docker Hub username
DOCKER_USERNAME=your_dockerhub_username
# Your Docker Hub password or access token (create at https://hub.docker.com/settings/security)
DOCKER_PASSWORD=your_dockerhub_token

# Aliyun Container Registry Configuration
# Your Aliyun ACR username (usually the full name of your Alibaba Cloud account)
ALIYUN_ACR_USERNAME=your_aliyun_acr_username
# Your Aliyun ACR password (create at https://cr.console.aliyun.com/)
ALIYUN_ACR_PASSWORD=your_aliyun_acr_password
# Aliyun ACR registry endpoint (default: registry.cn-hangzhou.aliyuncs.com)
# Other regions: registry.cn-beijing.aliyuncs.com, registry.cn-shanghai.aliyuncs.com, etc.
ALIYUN_ACR_REGISTRY=registry.cn-hangzhou.aliyuncs.com

# Azure Container Registry Configuration
# Your Azure ACR username (create at https://portal.azure.com/ -> Container Registry -> Access keys)
AZURE_ACR_USERNAME=your_azure_acr_username
# Your Azure ACR password
AZURE_ACR_PASSWORD=your_azure_acr_password
# Azure ACR registry endpoint (default: hagicode.azurecr.io)
AZURE_ACR_REGISTRY=hagicode.azurecr.io

# GitHub Configuration
# GitHub token is automatically provided in GitHub Actions, but you can set it for local testing
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# GitHub repository (e.g., owner/repo) - automatically set in GitHub Actions
GITHUB_REPOSITORY=newbe36524/hagicode-release
# Dry run mode for version monitor (set to 'true' to skip triggering releases)
DRY_RUN=false

# Build Configuration
# Docker registry for pushing images (default: docker.io/newbe36524)
DOCKER_REGISTRY=docker.io/newbe36524
# Docker image name (default: hagicode)
DOCKER_IMAGE_NAME=hagicode
# Version (usually extracted from git tag, can be set manually for testing)
VERSION=v1.0.0

# Output Directory
# Directory for downloaded packages and extracted files
OUTPUT_DIRECTORY=./output

# Docker Container Configuration (for Claude Code in container)
# Agent Teams multi-agent collaboration feature (default: 1, set to 0 or empty to disable)
# CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
# Sonnet layer model version for ZAI API (default: glm-4.7)
# ZAI_SONNET_MODEL=glm-4.7
# Opus layer model version for ZAI API (default: glm-4.7)
# ZAI_OPUS_MODEL=glm-4.7
